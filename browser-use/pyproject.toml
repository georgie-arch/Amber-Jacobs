[project]
name = "amber-browser-use"
version = "0.1.0"
description = "Browser Use runner for event registration workflows"
requires-python = ">=3.11"
dependencies = [
    "browser-use==0.12.6",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
